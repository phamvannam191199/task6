#include <stdio.h>

main(void)
{
	float x, y, z;

	printf("Nhap so x y z : \n");
	scanf("%f%f%f",&x, &y, &z);

	if(a>b&&a>c)
	 printf("Max = %f",x);

	if(b>a&&b>c)
	 printf("Max = %f",y);

	if(c>a&&c>b)
	 printf("Max = %f",z);
} 
